import { MessageWall } from "@/components/message-wall";
import { PageShell } from "@/components/page-shell";
export default function MessagesPage(){return <PageShell eyebrow="PUBLIC ROAD MESSAGES" title={<>India writes<br />back.</>} intro="Messages appear here as they arrive. Private contact details never do. Navneet answers every one himself and his reply publishes underneath the message it answers — so you can see, before you write, that people do get answered."><MessageWall /></PageShell>}
